{
  "name": "reject JavaScript array methods by default",
  "obj": {
    "groups": [
      {
        "name": "matching",
        "values": [
          1,
          20
        ]
      }
    ]
  },
  "expr": "$.groups[?(@.values.some(function (value) { return value > 10 }))].name",
  "expectedError": "Unsafe or unsupported token '('"
}
