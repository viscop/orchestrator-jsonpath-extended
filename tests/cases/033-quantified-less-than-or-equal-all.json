{
  "name": "select stores where no nested price is above the limit",
  "fixture": "stores-quantified.json",
  "expr": "$.stores[?(@.book[?(@)] && !@.book[?(@.price > 8)])].name",
  "expected": [
    "Budget Only"
  ]
}
