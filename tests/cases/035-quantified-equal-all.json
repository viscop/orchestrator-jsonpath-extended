{
  "name": "select stores where no nested price differs from the limit",
  "fixture": "stores-quantified.json",
  "expr": "$.stores[?(@.book[?(@)] && !@.book[?(@.price != 10)])].name",
  "expected": [
    "Fixed Price"
  ]
}
