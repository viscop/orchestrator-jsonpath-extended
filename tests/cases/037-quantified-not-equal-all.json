{
  "name": "select stores where no nested price equals the limit",
  "fixture": "stores-quantified.json",
  "expr": "$.stores[?(@.book[?(@)] && !@.book[?(@.price == 10)])].name",
  "expected": [
    "Mixed Prices",
    "Premium Only",
    "Budget Only"
  ]
}
