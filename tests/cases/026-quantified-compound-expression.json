{
  "name": "combine nested existence filters with boolean operators",
  "fixture": "stores-quantified.json",
  "expr": "$.stores[?(@.book[?(@.price > 10)] && !@.book[?(@.price <= 0)])].name",
  "expected": [
    "Mixed Prices",
    "Premium Only",
    "Mixed With Ten"
  ]
}
